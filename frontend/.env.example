# Frontend env variables (copy these into Vercel Project → Settings → Environment Variables)
# Note: Vercel only exposes vars prefixed with VITE_ to the client

# Base URL of your backend API, e.g. https://<railway-domain>/api
VITE_API_URL=

# WalletConnect (if used)
VITE_WALLET_CONNECT_PROJECT_ID=

# Hashvertise smart contract address (0x... or Hedera format)
VITE_HASHVERTISE_SMART_CONTRACT_ADDRESS=