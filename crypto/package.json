{
  "name": "@hashvertise/crypto",
  "version": "0.1.0",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "demo": "ts-node-dev --respawn --transpile-only src/scripts/demo.ts",
    "submit-messages": "ts-node-dev --respawn --transpile-only src/scripts/createTopicAndSubmitMessages.ts",
    "build": "tsc",
    "build:watch": "tsc --watch"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "peerDependencies": {
    "@hashgraph/sdk": "^2.63.0"
  },
  "devDependencies": {
    "@hashgraph/sdk": "^2.63.0",
    "typescript": "^4.9.5",
    "ts-node-dev": "^2.0.0",
    "@types/node": "^18.0.0"
  }
}
